# Path where HuggingFace downloads and caches model weights.
# On uni GPU nodes, point this at a storage path with sufficient space.
# Mistral-7B (4-bit) is ~4 GB. Llama 3 8B (4-bit) is ~5 GB.
HF_HOME=/path/to/hf_cache
